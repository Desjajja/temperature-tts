[project]
name = "temperature-tts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "math-verify[antlr4-13-2]>=0.8.0",
    "openai>=2.6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
